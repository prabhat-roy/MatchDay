apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: allow-istio-mesh, namespace: matchday-platform }
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector: { matchLabels: { istio-injection: enabled } }

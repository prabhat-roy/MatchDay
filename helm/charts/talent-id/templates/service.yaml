apiVersion: v1
kind: Service
metadata:
  name: {{ include "talent-id.fullname" . }}
  labels: {{- include "talent-id.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "talent-id.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "fixture-service.fullname" . }}
  labels: {{- include "fixture-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fixture-service.selectorLabels" . | nindent 4 }}

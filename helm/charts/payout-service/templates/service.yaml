apiVersion: v1
kind: Service
metadata:
  name: {{ include "payout-service.fullname" . }}
  labels: {{- include "payout-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "payout-service.selectorLabels" . | nindent 4 }}

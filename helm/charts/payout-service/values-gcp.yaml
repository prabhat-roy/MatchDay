cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: matchday-payout-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com

cloud: { provider: gcp, storageClass: pd-ssd, ingressClass: gce }
serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: matchday-fan-bff@{{ .Values.gcp.project }}.iam.gserviceaccount.com
envFrom:
  - secretRef: { name: fan-bff-gcp-secrets }

apiVersion: v1
kind: Service
metadata:
  name: {{ include "clip-publisher.fullname" . }}
  labels: {{- include "clip-publisher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "clip-publisher.selectorLabels" . | nindent 4 }}

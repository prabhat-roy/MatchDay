apiVersion: v1
kind: Service
metadata:
  name: {{ include "stream-service.fullname" . }}
  labels: {{- include "stream-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "stream-service.selectorLabels" . | nindent 4 }}

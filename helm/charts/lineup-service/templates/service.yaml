apiVersion: v1
kind: Service
metadata:
  name: {{ include "lineup-service.fullname" . }}
  labels: {{- include "lineup-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "lineup-service.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "squad-service.fullname" . }}
  labels: {{- include "squad-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "squad-service.selectorLabels" . | nindent 4 }}

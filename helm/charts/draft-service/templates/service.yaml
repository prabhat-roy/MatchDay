apiVersion: v1
kind: Service
metadata:
  name: {{ include "draft-service.fullname" . }}
  labels: {{- include "draft-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "draft-service.selectorLabels" . | nindent 4 }}

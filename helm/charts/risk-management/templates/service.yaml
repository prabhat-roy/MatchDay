apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-management.fullname" . }}
  labels: {{- include "risk-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "risk-management.selectorLabels" . | nindent 4 }}

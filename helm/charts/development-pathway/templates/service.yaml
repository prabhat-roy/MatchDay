apiVersion: v1
kind: Service
metadata:
  name: {{ include "development-pathway.fullname" . }}
  labels: {{- include "development-pathway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "development-pathway.selectorLabels" . | nindent 4 }}

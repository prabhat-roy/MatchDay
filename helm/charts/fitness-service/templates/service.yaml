apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitness-service.fullname" . }}
  labels: {{- include "fitness-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fitness-service.selectorLabels" . | nindent 4 }}

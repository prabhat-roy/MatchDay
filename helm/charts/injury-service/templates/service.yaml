apiVersion: v1
kind: Service
metadata:
  name: {{ include "injury-service.fullname" . }}
  labels: {{- include "injury-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "injury-service.selectorLabels" . | nindent 4 }}

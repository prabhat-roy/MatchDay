apiVersion: v1
kind: Service
metadata:
  name: {{ include "standings-service.fullname" . }}
  labels: {{- include "standings-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "standings-service.selectorLabels" . | nindent 4 }}

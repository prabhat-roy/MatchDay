apiVersion: v1
kind: Service
metadata:
  name: {{ include "var-service.fullname" . }}
  labels: {{- include "var-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "var-service.selectorLabels" . | nindent 4 }}

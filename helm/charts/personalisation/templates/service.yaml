apiVersion: v1
kind: Service
metadata:
  name: {{ include "personalisation.fullname" . }}
  labels: {{- include "personalisation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "personalisation.selectorLabels" . | nindent 4 }}

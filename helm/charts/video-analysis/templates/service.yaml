apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-analysis.fullname" . }}
  labels: {{- include "video-analysis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "video-analysis.selectorLabels" . | nindent 4 }}

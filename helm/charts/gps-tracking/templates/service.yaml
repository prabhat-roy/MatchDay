apiVersion: v1
kind: Service
metadata:
  name: {{ include "gps-tracking.fullname" . }}
  labels: {{- include "gps-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "gps-tracking.selectorLabels" . | nindent 4 }}

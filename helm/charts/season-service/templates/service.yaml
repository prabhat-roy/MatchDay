apiVersion: v1
kind: Service
metadata:
  name: {{ include "season-service.fullname" . }}
  labels: {{- include "season-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "season-service.selectorLabels" . | nindent 4 }}

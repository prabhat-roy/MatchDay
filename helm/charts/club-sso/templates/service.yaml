apiVersion: v1
kind: Service
metadata:
  name: {{ include "club-sso.fullname" . }}
  labels: {{- include "club-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "club-sso.selectorLabels" . | nindent 4 }}

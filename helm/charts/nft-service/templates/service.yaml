apiVersion: v1
kind: Service
metadata:
  name: {{ include "nft-service.fullname" . }}
  labels: {{- include "nft-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "nft-service.selectorLabels" . | nindent 4 }}

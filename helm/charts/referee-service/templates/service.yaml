apiVersion: v1
kind: Service
metadata:
  name: {{ include "referee-service.fullname" . }}
  labels: {{- include "referee-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "referee-service.selectorLabels" . | nindent 4 }}

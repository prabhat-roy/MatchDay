apiVersion: v1
kind: Service
metadata:
  name: {{ include "roster-service.fullname" . }}
  labels: {{- include "roster-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "roster-service.selectorLabels" . | nindent 4 }}

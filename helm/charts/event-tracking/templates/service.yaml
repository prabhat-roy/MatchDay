apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-tracking.fullname" . }}
  labels: {{- include "event-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "event-tracking.selectorLabels" . | nindent 4 }}

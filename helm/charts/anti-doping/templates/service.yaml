apiVersion: v1
kind: Service
metadata:
  name: {{ include "anti-doping.fullname" . }}
  labels: {{- include "anti-doping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "anti-doping.selectorLabels" . | nindent 4 }}

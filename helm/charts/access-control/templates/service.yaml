apiVersion: v1
kind: Service
metadata:
  name: {{ include "access-control.fullname" . }}
  labels: {{- include "access-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "access-control.selectorLabels" . | nindent 4 }}

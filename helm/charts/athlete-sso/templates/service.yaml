apiVersion: v1
kind: Service
metadata:
  name: {{ include "athlete-sso.fullname" . }}
  labels: {{- include "athlete-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "athlete-sso.selectorLabels" . | nindent 4 }}

cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: matchday-profile-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com

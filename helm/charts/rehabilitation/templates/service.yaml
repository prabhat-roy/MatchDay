apiVersion: v1
kind: Service
metadata:
  name: {{ include "rehabilitation.fullname" . }}
  labels: {{- include "rehabilitation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rehabilitation.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "biometric-service.fullname" . }}
  labels: {{- include "biometric-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "biometric-service.selectorLabels" . | nindent 4 }}

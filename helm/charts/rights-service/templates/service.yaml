apiVersion: v1
kind: Service
metadata:
  name: {{ include "rights-service.fullname" . }}
  labels: {{- include "rights-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rights-service.selectorLabels" . | nindent 4 }}

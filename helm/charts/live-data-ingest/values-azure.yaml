cloud: { provider: azure, storageClass: managed-csi-premium }
serviceAccount: { annotations: { azure.workload.identity/client-id: "{{ .Values.azure.clientId }}" } }
envFrom: [{ secretRef: { name: live-data-ingest-azure-secrets } }]

apiVersion: v1
kind: Service
metadata:
  name: {{ include "seat-map-service.fullname" . }}
  labels: {{- include "seat-map-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "seat-map-service.selectorLabels" . | nindent 4 }}

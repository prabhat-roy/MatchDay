apiVersion: v1
kind: Service
metadata:
  name: {{ include "fan-service.fullname" . }}
  labels: {{- include "fan-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fan-service.selectorLabels" . | nindent 4 }}

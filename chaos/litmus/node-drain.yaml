apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata: { name: matchday-node-drain, namespace: litmus }
spec:
  appinfo: { appns: matchday, applabel: "tier=api", appkind: deployment }
  chaosServiceAccount: litmus-admin
  experiments:
    - { name: node-drain, spec: { components: { env: [ { name: TOTAL_CHAOS_DURATION, value: "60" } ] } } }

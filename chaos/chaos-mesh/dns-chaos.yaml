apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata: { name: matchday-dns-error, namespace: chaos-mesh }
spec:
  action: error
  mode: all
  patterns: ["*.matchday.svc.cluster.local"]
  selector: { namespaces: [matchday] }
  duration: 2m

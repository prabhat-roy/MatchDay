apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata: { name: matchday-pod-kill, namespace: chaos-mesh }
spec:
  action: pod-kill
  mode: one
  selector: { namespaces: [matchday] }
  duration: 1m
  scheduler: { cron: "@every 1h" }

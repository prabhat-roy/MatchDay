apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: matchday-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [matchday] }
  timeOffset: "+500ms"
  duration: 3m

apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata: { name: require-matchday-labels }
spec:
  validationFailureAction: Enforce
  rules:
    - name: check-required-labels
      match:
        any: [{ resources: { kinds: [Deployment, StatefulSet] } }]
      validate:
        message: "missing required labels: app.kubernetes.io/name, matchday.io/domain"
        pattern:
          metadata:
            labels:
              app.kubernetes.io/name: "?*"
              matchday.io/domain: "?*"

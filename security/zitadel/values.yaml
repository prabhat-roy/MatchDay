# ZITADEL — developer-platform auth (API keys, OAuth clients, dev portal SSO)
image: { repository: ghcr.io/zitadel/zitadel, tag: latest }
zitadel:
  externalDomain: zitadel.matchday.internal
  externalSecure: true
  database: { postgres: { host: postgres.matchday.svc, database: zitadel } }
ingress: { enabled: true, hosts: [{ host: zitadel.matchday.internal }] }

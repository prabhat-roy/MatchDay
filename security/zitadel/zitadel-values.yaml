zitadel:
  masterkey: change-me-32-bytes-minimum
  configmapConfig:
    ExternalDomain: zitadel.matchday.internal
    ExternalSecure: true
    ExternalPort: 443
ingress:
  enabled: true
  hosts: [{ host: zitadel.matchday.internal }]

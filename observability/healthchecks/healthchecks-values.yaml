image: { repository: healthchecks/healthchecks, tag: latest }
postgresql: { enabled: true }
env:
  - { name: SITE_ROOT, value: https://hchk.matchday.internal }
ingress:
  enabled: true
  hosts: [{ host: hchk.matchday.internal }]

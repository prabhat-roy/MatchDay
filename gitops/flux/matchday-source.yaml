apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: matchday, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/MatchDay.git
  ref: { branch: main }

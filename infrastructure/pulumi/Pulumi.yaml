name: matchday
description: MatchDay — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://matchday-pulumi-state
config:
  matchday:env: prod
  matchday:cloud: aws

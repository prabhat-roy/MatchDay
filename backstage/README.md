# Backstage — Developer Portal for MatchDay

Self-hosted Spotify Backstage. Service catalog, TechDocs, software templates, scorecards.
Run: `yarn install && yarn dev`. Upstream: https://github.com/backstage/backstage

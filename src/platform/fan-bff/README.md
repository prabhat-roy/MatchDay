# fan-bff

BFF for fan portal (Next.js)

**Domain:** `platform` · **Language:** go · **Port:** 50001

## Run locally

```bash
make run
```

## Endpoints

- `GET /healthz` — health check, returns `{ "status": "ok" }`

apiVersion: v1
kind: Service
metadata:
  name: var-service
  namespace: matchday
  labels: { app.kubernetes.io/name: var-service, matchday.io/domain: match-ops }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: var-service }

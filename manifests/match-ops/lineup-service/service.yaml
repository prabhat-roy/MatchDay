apiVersion: v1
kind: Service
metadata:
  name: lineup-service
  namespace: matchday
  labels: { app.kubernetes.io/name: lineup-service, matchday.io/domain: match-ops }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50140, targetPort: 50140 }]
  selector: { app.kubernetes.io/name: lineup-service }

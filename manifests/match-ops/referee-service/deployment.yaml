apiVersion: apps/v1
kind: Deployment
metadata:
  name: referee-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: referee-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: match-ops
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: referee-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: referee-service, matchday.io/domain: match-ops }
    spec:
      serviceAccountName: referee-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: referee-service
          image: harbor.matchday.internal/matchday/referee-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: referee-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

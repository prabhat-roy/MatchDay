apiVersion: v1
kind: Service
metadata:
  name: referee-service
  namespace: matchday
  labels: { app.kubernetes.io/name: referee-service, matchday.io/domain: match-ops }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: referee-service }

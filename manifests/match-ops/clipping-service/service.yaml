apiVersion: v1
kind: Service
metadata:
  name: clipping-service
  namespace: matchday
  labels: { app.kubernetes.io/name: clipping-service, matchday.io/domain: match-ops }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50143, targetPort: 50143 }]
  selector: { app.kubernetes.io/name: clipping-service }

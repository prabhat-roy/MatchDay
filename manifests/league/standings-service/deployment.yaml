apiVersion: apps/v1
kind: Deployment
metadata:
  name: standings-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: standings-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: league
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: standings-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: standings-service, matchday.io/domain: league }
    spec:
      serviceAccountName: standings-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: standings-service
          image: harbor.matchday.internal/matchday/standings-service:latest
          ports: [{ containerPort: 50042, name: http }]
          envFrom: [{ configMapRef: { name: standings-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50042 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

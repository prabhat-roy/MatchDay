apiVersion: v1
kind: Service
metadata:
  name: standings-service
  namespace: matchday
  labels: { app.kubernetes.io/name: standings-service, matchday.io/domain: league }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50042, targetPort: 50042 }]
  selector: { app.kubernetes.io/name: standings-service }

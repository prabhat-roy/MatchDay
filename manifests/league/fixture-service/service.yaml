apiVersion: v1
kind: Service
metadata:
  name: fixture-service
  namespace: matchday
  labels: { app.kubernetes.io/name: fixture-service, matchday.io/domain: league }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: fixture-service }

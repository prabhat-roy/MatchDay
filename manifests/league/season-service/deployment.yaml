apiVersion: apps/v1
kind: Deployment
metadata:
  name: season-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: season-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: league
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: season-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: season-service, matchday.io/domain: league }
    spec:
      serviceAccountName: season-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: season-service
          image: harbor.matchday.internal/matchday/season-service:latest
          ports: [{ containerPort: 50040, name: http }]
          envFrom: [{ configMapRef: { name: season-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50040 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50040 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

apiVersion: v1
kind: Service
metadata:
  name: playoff-service
  namespace: matchday
  labels: { app.kubernetes.io/name: playoff-service, matchday.io/domain: league }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50043, targetPort: 50043 }]
  selector: { app.kubernetes.io/name: playoff-service }

apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitness-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: fitness-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: medicine
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fitness-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fitness-service, matchday.io/domain: medicine }
    spec:
      serviceAccountName: fitness-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fitness-service
          image: harbor.matchday.internal/matchday/fitness-service:latest
          ports: [{ containerPort: 50122, name: http }]
          envFrom: [{ configMapRef: { name: fitness-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

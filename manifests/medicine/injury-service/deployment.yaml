apiVersion: apps/v1
kind: Deployment
metadata:
  name: injury-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: injury-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: medicine
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: injury-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: injury-service, matchday.io/domain: medicine }
    spec:
      serviceAccountName: injury-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: injury-service
          image: harbor.matchday.internal/matchday/injury-service:latest
          ports: [{ containerPort: 50120, name: http }]
          envFrom: [{ configMapRef: { name: injury-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

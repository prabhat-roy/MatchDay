apiVersion: v1
kind: Service
metadata:
  name: injury-service
  namespace: matchday
  labels: { app.kubernetes.io/name: injury-service, matchday.io/domain: medicine }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: injury-service }

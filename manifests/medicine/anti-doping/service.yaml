apiVersion: v1
kind: Service
metadata:
  name: anti-doping
  namespace: matchday
  labels: { app.kubernetes.io/name: anti-doping, matchday.io/domain: medicine }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50123, targetPort: 50123 }]
  selector: { app.kubernetes.io/name: anti-doping }

apiVersion: v1
kind: Service
metadata:
  name: rehabilitation
  namespace: matchday
  labels: { app.kubernetes.io/name: rehabilitation, matchday.io/domain: medicine }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: rehabilitation }

apiVersion: apps/v1
kind: Deployment
metadata:
  name: kpi-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: kpi-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: performance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: kpi-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: kpi-service, matchday.io/domain: performance }
    spec:
      serviceAccountName: kpi-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: kpi-service
          image: harbor.matchday.internal/matchday/kpi-service:latest
          ports: [{ containerPort: 50103, name: http }]
          envFrom: [{ configMapRef: { name: kpi-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

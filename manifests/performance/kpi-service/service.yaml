apiVersion: v1
kind: Service
metadata:
  name: kpi-service
  namespace: matchday
  labels: { app.kubernetes.io/name: kpi-service, matchday.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50103, targetPort: 50103 }]
  selector: { app.kubernetes.io/name: kpi-service }

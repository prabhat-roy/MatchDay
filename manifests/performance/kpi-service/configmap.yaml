apiVersion: v1
kind: ConfigMap
metadata:
  name: kpi-service
  namespace: matchday
  labels: { app.kubernetes.io/name: kpi-service, matchday.io/domain: performance }
data:
  LOG_LEVEL: info
  PORT: "50103"

apiVersion: v1
kind: Service
metadata:
  name: video-analysis
  namespace: matchday
  labels: { app.kubernetes.io/name: video-analysis, matchday.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: video-analysis }

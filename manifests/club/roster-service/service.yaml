apiVersion: v1
kind: Service
metadata:
  name: roster-service
  namespace: matchday
  labels: { app.kubernetes.io/name: roster-service, matchday.io/domain: club }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: roster-service }

apiVersion: apps/v1
kind: Deployment
metadata:
  name: roster-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: roster-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: club
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: roster-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: roster-service, matchday.io/domain: club }
    spec:
      serviceAccountName: roster-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: roster-service
          image: harbor.matchday.internal/matchday/roster-service:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: roster-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

apiVersion: apps/v1
kind: Deployment
metadata:
  name: squad-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: squad-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: club
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: squad-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: squad-service, matchday.io/domain: club }
    spec:
      serviceAccountName: squad-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: squad-service
          image: harbor.matchday.internal/matchday/squad-service:latest
          ports: [{ containerPort: 50060, name: http }]
          envFrom: [{ configMapRef: { name: squad-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

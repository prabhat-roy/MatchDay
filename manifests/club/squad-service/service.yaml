apiVersion: v1
kind: Service
metadata:
  name: squad-service
  namespace: matchday
  labels: { app.kubernetes.io/name: squad-service, matchday.io/domain: club }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: squad-service }

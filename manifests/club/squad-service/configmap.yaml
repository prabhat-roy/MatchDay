apiVersion: v1
kind: ConfigMap
metadata:
  name: squad-service
  namespace: matchday
  labels: { app.kubernetes.io/name: squad-service, matchday.io/domain: club }
data:
  LOG_LEVEL: info
  PORT: "50060"

apiVersion: v1
kind: Service
metadata:
  name: transfer-service
  namespace: matchday
  labels: { app.kubernetes.io/name: transfer-service, matchday.io/domain: club }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50063, targetPort: 50063 }]
  selector: { app.kubernetes.io/name: transfer-service }

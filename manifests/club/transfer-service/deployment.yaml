apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: transfer-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: club
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: transfer-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: transfer-service, matchday.io/domain: club }
    spec:
      serviceAccountName: transfer-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: transfer-service
          image: harbor.matchday.internal/matchday/transfer-service:latest
          ports: [{ containerPort: 50063, name: http }]
          envFrom: [{ configMapRef: { name: transfer-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

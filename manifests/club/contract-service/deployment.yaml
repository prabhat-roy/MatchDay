apiVersion: apps/v1
kind: Deployment
metadata:
  name: contract-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: contract-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: club
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: contract-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: contract-service, matchday.io/domain: club }
    spec:
      serviceAccountName: contract-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: contract-service
          image: harbor.matchday.internal/matchday/contract-service:latest
          ports: [{ containerPort: 50062, name: http }]
          envFrom: [{ configMapRef: { name: contract-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

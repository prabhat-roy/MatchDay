apiVersion: v1
kind: Service
metadata:
  name: contract-service
  namespace: matchday
  labels: { app.kubernetes.io/name: contract-service, matchday.io/domain: club }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: contract-service }

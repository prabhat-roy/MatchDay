apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: matchday
  labels: { app.kubernetes.io/name: profile-service, matchday.io/domain: athlete }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: profile-service }

apiVersion: v1
kind: ConfigMap
metadata:
  name: profile-service
  namespace: matchday
  labels: { app.kubernetes.io/name: profile-service, matchday.io/domain: athlete }
data:
  LOG_LEVEL: info
  PORT: "50080"

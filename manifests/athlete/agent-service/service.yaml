apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: matchday
  labels: { app.kubernetes.io/name: agent-service, matchday.io/domain: athlete }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50082, targetPort: 50082 }]
  selector: { app.kubernetes.io/name: agent-service }

apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-service
  namespace: matchday
  labels: { app.kubernetes.io/name: agent-service, matchday.io/domain: athlete }
data:
  LOG_LEVEL: info
  PORT: "50082"

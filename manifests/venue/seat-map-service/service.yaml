apiVersion: v1
kind: Service
metadata:
  name: seat-map-service
  namespace: matchday
  labels: { app.kubernetes.io/name: seat-map-service, matchday.io/domain: venue }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: seat-map-service }

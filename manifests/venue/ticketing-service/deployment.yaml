apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticketing-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: ticketing-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: venue
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ticketing-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ticketing-service, matchday.io/domain: venue }
    spec:
      serviceAccountName: ticketing-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ticketing-service
          image: harbor.matchday.internal/matchday/ticketing-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: ticketing-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

apiVersion: v1
kind: Service
metadata:
  name: ticketing-service
  namespace: matchday
  labels: { app.kubernetes.io/name: ticketing-service, matchday.io/domain: venue }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: ticketing-service }

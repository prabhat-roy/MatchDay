apiVersion: v1
kind: ConfigMap
metadata:
  name: vip-service
  namespace: matchday
  labels: { app.kubernetes.io/name: vip-service, matchday.io/domain: venue }
data:
  LOG_LEVEL: info
  PORT: "50163"

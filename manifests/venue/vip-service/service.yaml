apiVersion: v1
kind: Service
metadata:
  name: vip-service
  namespace: matchday
  labels: { app.kubernetes.io/name: vip-service, matchday.io/domain: venue }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: vip-service }

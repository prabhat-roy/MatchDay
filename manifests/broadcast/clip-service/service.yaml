apiVersion: v1
kind: Service
metadata:
  name: clip-service
  namespace: matchday
  labels: { app.kubernetes.io/name: clip-service, matchday.io/domain: broadcast }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50183, targetPort: 50183 }]
  selector: { app.kubernetes.io/name: clip-service }

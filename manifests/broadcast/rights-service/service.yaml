apiVersion: v1
kind: Service
metadata:
  name: rights-service
  namespace: matchday
  labels: { app.kubernetes.io/name: rights-service, matchday.io/domain: broadcast }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: rights-service }

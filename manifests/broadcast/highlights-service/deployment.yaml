apiVersion: apps/v1
kind: Deployment
metadata:
  name: highlights-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: highlights-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: broadcast
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: highlights-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: highlights-service, matchday.io/domain: broadcast }
    spec:
      serviceAccountName: highlights-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: highlights-service
          image: harbor.matchday.internal/matchday/highlights-service:latest
          ports: [{ containerPort: 50182, name: http }]
          envFrom: [{ configMapRef: { name: highlights-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

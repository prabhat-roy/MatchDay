apiVersion: v1
kind: ConfigMap
metadata:
  name: inventory-service
  namespace: matchday
  labels: { app.kubernetes.io/name: inventory-service, matchday.io/domain: sponsorship }
data:
  LOG_LEVEL: info
  PORT: "50261"

apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: sponsorship
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: inventory-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: inventory-service, matchday.io/domain: sponsorship }
    spec:
      serviceAccountName: inventory-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: inventory-service
          image: harbor.matchday.internal/matchday/inventory-service:latest
          ports: [{ containerPort: 50261, name: http }]
          envFrom: [{ configMapRef: { name: inventory-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

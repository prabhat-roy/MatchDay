apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: matchday
  labels: { app.kubernetes.io/name: inventory-service, matchday.io/domain: sponsorship }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: inventory-service }

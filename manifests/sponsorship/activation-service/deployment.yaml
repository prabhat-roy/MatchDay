apiVersion: apps/v1
kind: Deployment
metadata:
  name: activation-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: activation-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: sponsorship
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: activation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: activation-service, matchday.io/domain: sponsorship }
    spec:
      serviceAccountName: activation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: activation-service
          image: harbor.matchday.internal/matchday/activation-service:latest
          ports: [{ containerPort: 50260, name: http }]
          envFrom: [{ configMapRef: { name: activation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

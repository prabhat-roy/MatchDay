apiVersion: v1
kind: Service
metadata:
  name: valuation-service
  namespace: matchday
  labels: { app.kubernetes.io/name: valuation-service, matchday.io/domain: sponsorship }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: valuation-service }

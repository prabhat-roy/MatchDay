apiVersion: v1
kind: Service
metadata:
  name: risk-management
  namespace: matchday
  labels: { app.kubernetes.io/name: risk-management, matchday.io/domain: betting }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: risk-management }

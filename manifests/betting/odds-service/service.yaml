apiVersion: v1
kind: Service
metadata:
  name: odds-service
  namespace: matchday
  labels: { app.kubernetes.io/name: odds-service, matchday.io/domain: betting }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: odds-service }

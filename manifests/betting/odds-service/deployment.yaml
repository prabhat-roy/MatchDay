apiVersion: apps/v1
kind: Deployment
metadata:
  name: odds-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: odds-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: betting
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: odds-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: odds-service, matchday.io/domain: betting }
    spec:
      serviceAccountName: odds-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: odds-service
          image: harbor.matchday.internal/matchday/odds-service:latest
          ports: [{ containerPort: 50240, name: http }]
          envFrom: [{ configMapRef: { name: odds-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

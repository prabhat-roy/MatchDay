apiVersion: v1
kind: ConfigMap
metadata:
  name: odds-service
  namespace: matchday
  labels: { app.kubernetes.io/name: odds-service, matchday.io/domain: betting }
data:
  LOG_LEVEL: info
  PORT: "50240"

apiVersion: apps/v1
kind: Deployment
metadata:
  name: loyalty-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: loyalty-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: fan-engagement
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: loyalty-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: loyalty-service, matchday.io/domain: fan-engagement }
    spec:
      serviceAccountName: loyalty-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: loyalty-service
          image: harbor.matchday.internal/matchday/loyalty-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: loyalty-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

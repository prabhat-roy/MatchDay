apiVersion: v1
kind: Service
metadata:
  name: loyalty-service
  namespace: matchday
  labels: { app.kubernetes.io/name: loyalty-service, matchday.io/domain: fan-engagement }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: loyalty-service }

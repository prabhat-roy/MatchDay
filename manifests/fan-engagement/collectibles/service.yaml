apiVersion: v1
kind: Service
metadata:
  name: collectibles
  namespace: matchday
  labels: { app.kubernetes.io/name: collectibles, matchday.io/domain: fan-engagement }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: collectibles }

apiVersion: v1
kind: Service
metadata: { name: watch-party, namespace: matchday-fan-engagement }
spec:
  clusterIP: None
  ports: [{ port: 4000, targetPort: http, name: http }]
  selector: { app.kubernetes.io/name: watch-party }
---
apiVersion: v1
kind: ServiceAccount
metadata: { name: watch-party, namespace: matchday-fan-engagement }

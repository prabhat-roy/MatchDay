apiVersion: v1
kind: Service
metadata:
  name: nft-service
  namespace: matchday
  labels: { app.kubernetes.io/name: nft-service, matchday.io/domain: fan-engagement }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: nft-service }

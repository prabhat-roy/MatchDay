apiVersion: v1
kind: Service
metadata:
  name: fulfilment-service
  namespace: matchday
  labels: { app.kubernetes.io/name: fulfilment-service, matchday.io/domain: merchandise }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: fulfilment-service }

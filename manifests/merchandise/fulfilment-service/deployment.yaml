apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulfilment-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: fulfilment-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: merchandise
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fulfilment-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fulfilment-service, matchday.io/domain: merchandise }
    spec:
      serviceAccountName: fulfilment-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fulfilment-service
          image: harbor.matchday.internal/matchday/fulfilment-service:latest
          ports: [{ containerPort: 50281, name: http }]
          envFrom: [{ configMapRef: { name: fulfilment-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

apiVersion: apps/v1
kind: Deployment
metadata:
  name: scouting-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: scouting-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: academy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: scouting-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: scouting-service, matchday.io/domain: academy }
    spec:
      serviceAccountName: scouting-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: scouting-service
          image: harbor.matchday.internal/matchday/scouting-service:latest
          ports: [{ containerPort: 50300, name: http }]
          envFrom: [{ configMapRef: { name: scouting-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

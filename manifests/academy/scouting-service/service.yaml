apiVersion: v1
kind: Service
metadata:
  name: scouting-service
  namespace: matchday
  labels: { app.kubernetes.io/name: scouting-service, matchday.io/domain: academy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: scouting-service }

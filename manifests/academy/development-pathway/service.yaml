apiVersion: v1
kind: Service
metadata:
  name: development-pathway
  namespace: matchday
  labels: { app.kubernetes.io/name: development-pathway, matchday.io/domain: academy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: development-pathway }

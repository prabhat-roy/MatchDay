apiVersion: v1
kind: Service
metadata:
  name: contest-service
  namespace: matchday
  labels: { app.kubernetes.io/name: contest-service, matchday.io/domain: fantasy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: contest-service }

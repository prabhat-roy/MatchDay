apiVersion: apps/v1
kind: Deployment
metadata:
  name: draft-service
  namespace: matchday
  labels:
    app.kubernetes.io/name: draft-service
    app.kubernetes.io/part-of: matchday
    matchday.io/domain: fantasy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: draft-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: draft-service, matchday.io/domain: fantasy }
    spec:
      serviceAccountName: draft-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: draft-service
          image: harbor.matchday.internal/matchday/draft-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: draft-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }

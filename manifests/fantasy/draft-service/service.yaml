apiVersion: v1
kind: Service
metadata:
  name: draft-service
  namespace: matchday
  labels: { app.kubernetes.io/name: draft-service, matchday.io/domain: fantasy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: draft-service }

apiVersion: v1
kind: Service
metadata:
  name: payout-service
  namespace: matchday
  labels: { app.kubernetes.io/name: payout-service, matchday.io/domain: fantasy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50223, targetPort: 50223 }]
  selector: { app.kubernetes.io/name: payout-service }

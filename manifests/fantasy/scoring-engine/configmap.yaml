apiVersion: v1
kind: ConfigMap
metadata:
  name: scoring-engine
  namespace: matchday
  labels: { app.kubernetes.io/name: scoring-engine, matchday.io/domain: fantasy }
data:
  LOG_LEVEL: info
  PORT: "50221"

apiVersion: v1
kind: Service
metadata:
  name: scoring-engine
  namespace: matchday
  labels: { app.kubernetes.io/name: scoring-engine, matchday.io/domain: fantasy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: scoring-engine }

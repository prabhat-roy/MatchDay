apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: matchday
  labels: { app.kubernetes.io/name: geo-address-service, matchday.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"

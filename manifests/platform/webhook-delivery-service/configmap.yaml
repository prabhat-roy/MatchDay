apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: matchday
  labels: { app.kubernetes.io/name: webhook-delivery-service, matchday.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"

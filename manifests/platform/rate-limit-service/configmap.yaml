apiVersion: v1
kind: ConfigMap
metadata:
  name: rate-limit-service
  namespace: matchday
  labels: { app.kubernetes.io/name: rate-limit-service, matchday.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51007"

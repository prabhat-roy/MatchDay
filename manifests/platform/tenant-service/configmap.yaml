apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-service
  namespace: matchday
  labels: { app.kubernetes.io/name: tenant-service, matchday.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51006"

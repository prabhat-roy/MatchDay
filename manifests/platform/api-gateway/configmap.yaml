apiVersion: v1
kind: ConfigMap
metadata: { name: api-gateway, namespace: matchday-platform, labels: { app.kubernetes.io/name: api-gateway, matchday.io/domain: platform } }
data:
  LOG_LEVEL: info
  PORT: "50000"

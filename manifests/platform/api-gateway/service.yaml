apiVersion: v1
kind: Service
metadata: { name: api-gateway, namespace: matchday-platform, labels: { app.kubernetes.io/name: api-gateway } }
spec:
  type: ClusterIP
  ports: [{ port: 50000, targetPort: http, name: http }]
  selector: { app.kubernetes.io/name: api-gateway }

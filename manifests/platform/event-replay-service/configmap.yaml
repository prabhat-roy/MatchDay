apiVersion: v1
kind: ConfigMap
metadata:
  name: event-replay-service
  namespace: matchday
  labels: { app.kubernetes.io/name: event-replay-service, matchday.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51009"

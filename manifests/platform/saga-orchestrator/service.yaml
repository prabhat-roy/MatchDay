apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator
  namespace: matchday
  labels: { app.kubernetes.io/name: saga-orchestrator, matchday.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51001, targetPort: 51001 }]
  selector: { app.kubernetes.io/name: saga-orchestrator }

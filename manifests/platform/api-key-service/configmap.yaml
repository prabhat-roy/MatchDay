apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: matchday
  labels: { app.kubernetes.io/name: api-key-service, matchday.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
